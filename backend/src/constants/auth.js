export const USER_ROLE = Object.freeze({ ADMIN: 'ADMIN', MECHANIC: 'MECANICO' });
export const USER_ROLES = Object.freeze(Object.values(USER_ROLE));

export const REFRESH_COOKIE_NAME = 'pavas_refresh_token';
export const REFRESH_COOKIE_PATH = '/api/auth';
