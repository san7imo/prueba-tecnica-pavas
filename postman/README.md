# Postman

The Postman collection is intentionally deferred until business endpoints are implemented. The final collection will mirror the real API, authentication flow, role restrictions and example assessment workflow; it will not describe aspirational endpoints as available.

